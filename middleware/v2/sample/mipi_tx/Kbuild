#
# Kbuild for panel
#
ifeq ($(CONFIG_MIPI_PANEL_HX8394), y)
    KBUILD_DEFINES += -DMIPI_PANEL_HX8394
endif

ifeq ($(CONFIG_MIPI_PANEL_ILI9881C), y)
    KBUILD_DEFINES += -DMIPI_PANEL_ILI9881C
endif

ifeq ($(CONFIG_MIPI_PANEL_ILI9881D), y)
    KBUILD_DEFINES += -DMIPI_PANEL_ILI9881D
endif

ifeq ($(CONFIG_MIPI_PANEL_JD9366AB), y)
    KBUILD_DEFINES += -DMIPI_PANEL_JD9366AB
endif

ifeq ($(CONFIG_MIPI_PANEL_NT35521), y)
    KBUILD_DEFINES += -DMIPI_PANEL_NT35521
endif

ifeq ($(CONFIG_MIPI_PANEL_OTA7290B), y)
    KBUILD_DEFINES += -DMIPI_PANEL_OTA7290B
endif

ifeq ($(CONFIG_MIPI_PANEL_OTA7290B_1920), y)
    KBUILD_DEFINES += -DMIPI_PANEL_OTA7290B_1920
endif

ifeq ($(CONFIG_MIPI_PANEL_ICN9707), y)
    KBUILD_DEFINES += -DMIPI_PANEL_ICN9707
endif

ifeq ($(CONFIG_MIPI_PANEL_3AML069LP01G), y)
    KBUILD_DEFINES += -DMIPI_PANEL_3AML069LP01G
endif

ifeq ($(CONFIG_MIPI_PANEL_ST7701), y)
    KBUILD_DEFINES += -DMIPI_PANEL_ST7701
endif

ifeq ($(CONFIG_MIPI_PANEL_4_LANES), y)
    KBUILD_DEFINES += -DMIPI_PANEL_4_LANES
endif

ifeq ($(CONFIG_MIPI_PANEL_2_LANES), y)
    KBUILD_DEFINES += -DMIPI_PANEL_2_LANES
endif


